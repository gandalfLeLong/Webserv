server {
    listen      8080;                   # The port the server listens on
    server_name example.com;        # The domain name or IP address for the server

    # Location block for static file serving
    location / {
        root /var/www/html;        # Directory to serve files from
        index index.html;           # Default file to serve
    }
}
